import React from 'react';

const styles = {
  header: {
    display: 'flex',
    justifyContent: 'center',
    width: '100%'
  }
};

const About = () => {
  return (
    <div>
      <h1 style={styles.header}>About</h1>
    </div>
  );
};

export default About;
