import React from 'react';

const styles = {
  header: {
    display: 'flex',
    justifyContent: 'center',
    width: '100%'
  }
};

const Contact = () => {
  return (
    <div>
      <h1 style={styles.header}>Contact</h1>
    </div>
  );
};

export default Contact;
